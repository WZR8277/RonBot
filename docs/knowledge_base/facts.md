# Agent/AI 前沿 — 可靠事实库

（本文件为示例，可复制到 workspace/knowledge_base/facts.md 并按需增删。）

## 用途

供定时「Agent 前沿周报」任务检索与追加：已知框架、术语、重要进展的时间线，便于去重与对比。

## 已知术语与框架（示例）

- **ReAct**: 推理+行动范式，LLM 交替生成推理与工具调用。
- **Tool Use / Function Calling**: 模型输出结构化工具调用，由运行时执行。
- **LangChain / LlamaIndex / CrewAI** 等：常见 Agent 编排与 RAG 框架。
- **Benchmark**: SWE-bench、AgentBench、WebArena 等用于评估 Agent 能力。

## 时间线（示例）

- 截至 2025 年初，主流方向包括：多步推理、多模态 Agent、与 MCP 等工具协议集成。

（以下由定时任务自动追加可靠进展，格式：日期、来源、摘要。）
